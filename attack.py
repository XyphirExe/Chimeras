from effect import Effect
from type import Type

class Attack:

    def __init__(self, name: str, power: int, precision: float, critRate: float, type: Type, effects: list[Effect]) -> None:
        pass